{
  "name": "landing",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Landing estática Astro — sin Tailwind, sin React, JavaScript mínimo. Build → copia a public/landing/.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node ./scripts/copy-dist.mjs",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^7.1.6"
  }
}
