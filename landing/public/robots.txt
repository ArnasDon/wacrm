# robots.txt — landing del fork WACRM (plantilla, NO es una clínica real).
#
# Mientras site.ts conserve placeholders (clinicName "Tu Clínica",
# whatsappNumber falso, baseUrl wacrm.tech — todos con TODO dueño),
# la landing NO debe indexarse: evita que Google indexe "Tu Clínica"
# con un teléfono que no existe.
#
# Cuando el fork se despliegue con datos reales (site.ts completado),
# cambiar a:
#   User-agent: *
#   Allow: /
# y añadir sitemap.xml si aplica.
User-agent: *
Disallow: /
