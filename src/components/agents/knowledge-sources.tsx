'use client'

/**
 * Legacy NOVURA configuration UI.
 *
 * This component is intentionally kept as a compatibility stub so older
 * imports/build artefacts do not break, but NOVURA is no longer exposed in
 * the Agents screen. External business data should be modelled as tools or
 * catalogue sources instead of being mixed with the company's knowledge base.
 */
export function KnowledgeSources() {
  return null
}
